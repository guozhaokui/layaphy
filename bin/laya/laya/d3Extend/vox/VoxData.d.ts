export declare class VoxData {
    private sizex;
    private sizey;
    private sizez;
    voxels: number[];
    count: number;
    constructor(_voxels: Uint8Array, xsize: number, ysize: number, zsize: number, ColorPlane: number);
}
